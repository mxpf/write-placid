# Site security boundary

The site is a static build of trusted source files committed to this repository. It does not accept visitor uploads or execute Markdown at request time. Draft content is excluded unless `INCLUDE_DRAFTS=1` is explicitly set for a staging build.
